export interface Mail {
  id: any;
  name: string;
  message: string;
  isRead: boolean;
  date: string;
  isStar: boolean;
  time: string;
  userProfile: string;
  heroheading: string;
  mailid: string;
  image?: string;
}
